<Window x:Class="Weather.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Weather"
        mc:Ignorable="d"
       Title="Weather App" Height="250" Width="400">
   <Grid Margin="10">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Enter Longitude:" Margin="5"/>
    <TextBox x:Name="longitudeTextBox" Grid.Row="1" Margin="5" Padding="5" Width="200" VerticalAlignment="Center"/>

    <TextBlock Grid.Row="2" Text="Enter Latitude:" Margin="5"/>
    <TextBox x:Name="latitudeTextBox" Grid.Row="3" Margin="5" Padding="5" Width="200" VerticalAlignment="Center"/>

    <Button Content="Get Weather" Grid.Row="4" Margin="5" Click="GetWeather_Click" HorizontalAlignment="Center" Padding="10"/>

    <Border Grid.Row="5" BorderThickness="1" BorderBrush="DarkGray" Margin="5" Padding="10">
        <TextBlock x:Name="weatherInfoTextBlock" Margin="0"/>
    </Border>
</Grid>

</Window>
